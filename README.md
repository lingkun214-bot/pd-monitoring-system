# PD 局放在线监测系统 HTML 原型

## 打开方式

直接双击本目录下的：

```text
index.html
```

无需安装依赖或启动服务。

## 登录

- 账号：`admin`
- 密码：`12345`
- 登录状态会保存在浏览器本地，刷新页面后仍保持登录。
- 点击“退出登录”可清除登录状态并返回登录页。

## 页面

系统包含登录页，以及以下 8 个主页面：

- 实时监测驾驶舱
- 历史数据分析
- 长周期趋势分析
- 智能诊断中心
- 数据处理
- 报警管理
- 设备管理
- 系统设置

## 关键功能

- AI 水轮发电机背景使用项目内的本地图片资产，不依赖外部网络。
- 历史数据支持组合查询、动态回放及 CSV、JSON、波形 JSON 导出；趋势目标与摘要、图表和导出数据保持联动。
- 智能诊断使用同一诊断上下文生成预览、可打印 HTML 报告，并保留审核人、签名和日期。
- 数据处理支持滤波参数校验、保存，以及相位屏蔽窗的新增、编辑、启停、删除和重叠校验。
- 报警管理支持机组、等级、状态组合筛选，并按“未确认—已派发—已确认—已关闭”流程处置。
- 设备配置、阈值、老化系数、IRIS 基准、演示用户和系统日志均可操作并保存到浏览器 `localStorage`。
- 系统设置支持可取消、失败模拟和重试的硬件自检；系统日志支持筛选、详情和 CSV 导出。
- 顶部报警铃铛显示未确认报警数量；底部状态栏显示数据更新时间及过期提醒。

## 设备导航范围

- 保留设备树：历史数据分析、长周期趋势分析、数据处理。
- 不显示设备树：实时监测、智能诊断、报警管理、设备管理、系统设置。
- 智能诊断使用模块内的机组和通道条件作为诊断目标。

## 原型边界

本项目是纯前端静态原型，无后端服务。以下能力明确留待生产环境接入：

- 服务端身份认证、密码安全策略、会话管理和 RBAC 权限控制；用户管理目前只演示资料与启停状态，登录仍固定为 `admin / 12345`。
- 真实传感器、采集卡、硬件自检接口及实时数据流；当前自检和数据新鲜度为交互演示。
- 数据库持久化、多用户并发、不可篡改审计日志和服务端报警工作流。
- 正式 PDF 报告生成、电子签名、文件归档和下载权限；当前下载为可打印 HTML 报告。
- 生产环境 API 的超时、断线重连、权限异常和跨设备数据一致性处理。

## 本地检查

如已安装 Node.js，可运行：

```text
node --test tests/core.test.js tests/structure.test.js
```
